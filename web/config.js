// Endereço do servidor do Zeny (Cloudflare Worker), que guarda a chave da API.
// Depois de publicar o servidor, coloque aqui a URL, ex.:
//   serverUrl: 'https://zeny-server.SEU-USUARIO.workers.dev'
// Vazio = o app funciona só no modo local (sem IA na nuvem).
window.ZENY_CONFIG = {
  serverUrl: '',
};
